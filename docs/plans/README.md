# Execution Plans

Use `active/YYYY-MM-DD-<slug>.md` for new work and move verified plans to `completed/`.

Plans must identify which D1 capability is changed, how non-D1 sources are protected, targeted test
coverage, and whether a multi-step write is atomic or best-effort.

